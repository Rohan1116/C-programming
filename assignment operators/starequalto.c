#include <stdio.h>

int main() {
  int x = 5;
  x *= 2;
  printf("%d", x);
  return 0;
  //here the value of is read by the system then it is multiplied by the value of *= and the output is presented as the x value.
}