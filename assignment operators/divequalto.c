#include <stdio.h>

int main() {
  float x = 5;
  x /= 2;
  printf("%f", x);
  return 0;
  //here the value of x is first taken and then it is divided by the /= and then the output is updated to the value of x.
}