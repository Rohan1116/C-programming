#include <stdio.h>

int main() {
  int x = 5;
  int y = 4;
  
 printf("%d", x >= y);
  return 0;
  //if x is greater than equal to y then the output will be equal to 1. Otherwise the output would be zero.
}