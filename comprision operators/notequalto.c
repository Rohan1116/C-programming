#include <stdio.h>

int main() {
  int x = 9;
  int y = 10;
  printf("%d", x != y); 
  return 0;
  //if the output is true then the output will be 1. and if the output is false it will return 0.
  //In this case 5 is not equal to 3 so it will give the output of 1.
}