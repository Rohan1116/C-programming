#include <stdio.h>

int main() {
  int x = 5;
  x -= 1;
  printf("%d", x);
  return 0;
  //here the value of x is read by the system and then number in -= is subtracted from the x value and the final answer is presented in the output.
}