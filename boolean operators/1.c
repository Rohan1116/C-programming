#include <stdio.h>
#include <stdbool.h>

int main() {
  bool isProgrammingFun = true;
  bool isdrawingfun = false;
  printf("%d\n", isProgrammingFun);
  printf("%d", isdrawingfun);
  
  return 0;
  //here in the boolean function the ouput of true is any number other than 0 but if the statemen is false then the output is 0.
}