#include <stdio.h>

int main() {
  printf("%d\n", 10 == 10);
  printf("%d\n", 10 == 15);
  printf("%d", 5 == 55);
  
  return 0;
  //here in this code if both the numbers are same then only the ouput will be shown as 1. Otherwise the output would be 0.
}
