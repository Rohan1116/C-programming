#include <stdio.h>

int main() {
  int x = 2;
  int y = 3;
  printf("%d", x < y);
  return 0;
  //in this case if x is less than y it will give an output of 1.
  //otherwise it will give an output of 0.
}